# Copy to .env.local for local dev, or paste into Vercel → Settings →
# Environment Variables for deployment. VITE_* vars are embedded in the
# built JS bundle — only put PUBLISHABLE values here, never secret keys.
#
# Both of these have hardcoded fallbacks in src/lib/config.js, so the app
# works even without a .env file — this is for overriding to a different
# Supabase project, or just being explicit in deployment.

VITE_SUPABASE_URL=https://hniplnaohvcbtmelatnz.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImhuaXBsbmFvaHZjYnRtZWxhdG56Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ1Nzk0MjAsImV4cCI6MjA4MDE1NTQyMH0.g7sgeZBW0RKkMI1lryA96Sym6cnejUAcmIx_npGr1Ko

# Optional: enables the AI Insight card on the dashboard
VITE_GEMINI_API_KEY=
